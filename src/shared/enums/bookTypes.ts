export enum BookProtection {
  WATERMARK = 1,
}

export enum BookBindingKind {
  HARDCOVER = 1,
  PAPERBACK = 2,
  NOTEBOOK = 3,
  SPIRAL = 4,
}

export enum BookType {
  EBOOK = 1,
  PAPER = 2,
  AUDIOBOOK = 3,
}

export enum SortMode {
  ACCURACY = 1,
  POPULARITY = 2,
  ALPHABETIC = 3,
}
