{
  "apps" : [
    {
      "name" : "upolujksiazke.pl",
      "script" : "dist/server.js",
      "instances" : "4",
      "exec_mode" : "cluster"
    }
  ],
  "deploy" : {
    "production" : {
      "user" : "deploy",
      "host" : ["192.166.218.41"],
      "ref"  : "origin/main",
      "repo" : "git@github.com:upolujksiazke/upolujksiazke.pl.git",
      "path" : "/var/www/upolujksiazke.pl",
      "post-deploy" : "yarn install --production=false && yarn run build:production && ln -sf ../.env ./.env && ln -sf ../.pgpass ./.pgpass && yarn run migration:run && pm2 startOrRestart ecosystem.json --env production",
      "env": {
        "NODE_ENV": "production",
        "APP_ENV": "production"
      }
    }
  }
}
